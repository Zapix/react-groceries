import { expect } from 'chai';
import { Map, fromJS } from 'immutable';
import { application } from '../../lib/reducers';
import * as constants from '../../lib/constants';

describe('Application Reducers', () => {
});
